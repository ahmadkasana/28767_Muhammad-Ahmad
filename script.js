/**
 * Simple Portfolio JavaScript
 * Uses basic concepts: variables, functions, click events
 */

// Toggle the mobile menu when Menu button is clicked
function toggleMenu() {
  var navLinks = document.getElementById("navLinks");
  navLinks.classList.toggle("show");
}
